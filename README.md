# Web content mining
used scrapy spiders
k-means clustering for clustering
flask for webapp

## running web app
1. **install dependencies**
    ```bash
    pip install -r requirements.txt
    ```

2.**runn app** 
    ```bash
    python app.py
    ```

